# SkillBridge AI Full Stack — HTML Frontend Edition

`backend/` is the existing FastAPI backend.
`frontend/` is the new HTML5 + Vanilla JS + Tailwind frontend.
`frontend-nextjs-backup/` preserves the previous Next.js frontend for reference.
